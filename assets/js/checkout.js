(() => {const cart=JSON.parse(localStorage.getItem("huevoke-cart")||"[]"),money=n=>new Intl.NumberFormat("en-IN",{style:"currency",currency:"INR",maximumFractionDigits:0}).format(n),$=s=>document.querySelector(s);if(!cart.length){location.replace("cart.html");return}$("#checkout-items").innerHTML=cart.map(x=>`<div class="checkout-item"><img src="${x.image}" alt=""><div><strong>${x.name}</strong><span>${x.size} × ${x.qty}</span></div><b>${money(x.price*x.qty)}</b></div>`).join("");$("#checkout-total").textContent=money(cart.reduce((s,x)=>s+x.price*x.qty,0));$("#order-field").value=cart.map(x=>`${x.name} | ${x.size} | Qty ${x.qty} | ${money(x.price*x.qty)}`).join("\n")})();

